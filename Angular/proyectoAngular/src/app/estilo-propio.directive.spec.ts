import { EstiloPropioDirective } from './estilo-propio.directive';

describe('EstiloPropioDirective', () => {
  it('should create an instance', () => {
    const directive = new EstiloPropioDirective();
    expect(directive).toBeTruthy();
  });
});
